import React, { Component } from 'react';
import { Link } from 'react-router-dom';

export default class User extends Component {

    render(){
        return (
            <div>
                This is the user page. This will show details related to the one particular user. For example, their emotes and account status.
            </div>
        )
    }

}